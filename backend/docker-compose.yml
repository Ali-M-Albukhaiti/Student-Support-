services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: student-support-backend
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      # Connect to FHICT SQL Server directly
      - ConnectionStrings__DefaultConnection=Server=mssqlstud.fhict.local;Database=dbi544850_ssupport;User Id=dbi544850_ssupport;Password=Hallo123!;TrustServerCertificate=True;Encrypt=False
    ports:
      - "5000:8080"   # Host 5000 -> Container 8080
      - "5001:8081"   # Host 5001 -> Container 8081
    networks:
      - student-support-network

networks:
  student-support-network:
    driver: bridge
